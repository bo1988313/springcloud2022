spring:
  application:
    name: ali-nacos-center
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        file-extension: yaml
        server-addr: localhost:8848
#management:
#  endpoints:
#    web:
#      exposure:
#        include: '*'
