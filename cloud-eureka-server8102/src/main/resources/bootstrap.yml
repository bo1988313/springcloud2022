spring:
  cloud:
    config:
      uri: http://localhost:3344
      label: master
      name: cloudEurekaService
      profile: dev
#暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"